@model Kalitte.Models.IHeaderedPageModel
@if (Model.Rendered)
{
    <section id="inner-headline">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                    <h2 class="pageTitle" style="font-size:@Model.HeaderFontSize">@Model.HeaderPageText</h2>
                </div>
            </div>
        </div>
    </section>
}
